# bevy-template

A small template for new projects using the [Bevy](https://bevy.org/) engine!

# Usage:

```sh
# Install cargo-generate, only needed once
cargo install cargo-generate

# Create a new project using the template
cargo generate cookie1170/bevy-template
```
